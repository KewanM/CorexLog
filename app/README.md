# App Directory

Application source is currently implemented in the `cortexlog/` Python package.
The `app/` directory is present to align the repository with the standard project
layout used in team workflow documentation.
